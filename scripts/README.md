# Database migration scripts

- `db-add-migration.sh <MigrationName>`: create a new EF Core migration.
- `db-migrate.sh`: apply pending migrations to the configured database.

These scripts are intended for local/dev and CI usage.

